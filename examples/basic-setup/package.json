{
  "name": "cdp-mcp-basic-example",
  "version": "1.0.0",
  "type": "module",
  "description": "Basic example of using vite-plugin-cdp-mcp",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "start-chrome": "google-chrome --remote-debugging-port=9222 --no-first-run --no-default-browser-check"
  },
  "dependencies": {
    "vite": "^7.1.4"
  },
  "devDependencies": {
    "vite-plugin-cdp-mcp": "^0.1.0"
  }
}
